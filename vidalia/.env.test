# Flask configuration for testing
SECRET_KEY=test_secret_key

# Security Onion API configuration for testing
# Using mock URL to prevent real API connections
SO_API_URL=https://mock-so-api
SO_CLIENT_ID=test_client_id
SO_CLIENT_SECRET=test_client_secret

# Disable SSL verification for tests
SO_VERIFY_SSL=false

# Short cache TTL for testing
USER_CACHE_TTL=0.1

# Test user credentials
SO_USERNAME=test_user
SO_PASSWORD=test_password
