export enum ChatUserRole {
    USER = "user",
    BASIC = "basic",
    ADMIN = "admin"
}

export enum ChatService {
    DISCORD = "DISCORD",
    SLACK = "SLACK",
    MATRIX = "MATRIX",
    TEAMS = "TEAMS"
}

export enum CommandPermission {
    PUBLIC = "public",
    BASIC = "basic",
    ADMIN = "admin"
}
